"""PROJECT Ω (OMEGA) — a computational substrate for open-ended organization.

This package is a *research framework*, not a finished simulator. It exists to
falsify one hypothesis:

    Open-ended increase of organizational complexity does not require infinite
    space. It requires infinite *constructibility* — organizations that can
    build organizations (and eventually operators) that were impossible before.

Everything here derives from a single object, the ``Organization``: a persistent
arrangement of interacting differences. Space, time, matter and life are not
primitives; if they appear at all they must appear as organizations.

See ``docs/ARCHITECTURE.md`` for the design and ``docs/RESEARCH_LOG.md`` for the
scientific narrative, including why this line departs from the earlier CA/Lenia
prototypes in the parent directory.
"""
from __future__ import annotations

__version__ = "0.1.0"
__all__ = ["__version__"]
